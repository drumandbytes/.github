version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    # Only offer releases at least a week old, so a hijacked or broken
    # release is usually pulled before it lands.
    cooldown:
      default-days: 7
    groups:
      actions:
        patterns: ["*"]
